from django.apps import AppConfig


class UsersConfig(AppConfig):
    name = 'users'
    # default_auto_field = 'django.db.models.BigAutoField'
    # name = 'users'

    # def ready(self):
    #     from django.contrib import admin
    #     admin.site.index_template = 'admin/index_no_recent.html'
